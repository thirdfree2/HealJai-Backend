<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Psychologist Details</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
            crossorigin="anonymous">
    </head>
    <body>
        <div class="container border p-4" style="margin-top: 50px;">
            <div class="d-flex justify-content-between">
                <h1>Psychologist Profile</h1>
                <a href="/admin/dashboard" class="btn btn-secondary h-50">กลับ</a>
            </div>
            <div class="profile border p-4 mt-4">
                <h2>UserName: <%= psychonistData.UserName %> UserID: <%= psychonistData.UserID %></h2>
                <p>Email: <%= psychonistData.Email %></p>
                <p>Name: <%= psychonistData.FirstName %> <%=psychonistData.LastName %> </p>
                <p>Address: <%= psychonistData.Address %></p>
                <p>Tel: <%= psychonistData.Tel %></p>
                <p>CreateDate: <%= psychonistData.CreateTime %></p>
                <a href="/admin/edit/<%= psychonistData.UserID %>"
                    class="btn btn-success edit">edit</a>
            </div>
            <div class="container border p-4 mt-4">
                <h2>เอกสารที่เกี่ยวข้อง</h2>
                <div class="row">
                    <% for(let i=0; i < attachments.length; i++) { %>
                    <div class="col-md-4">
                        <div class="card mb-4">
                            <img src="/uploads/<%= attachments[i].FileName %>" class="card-img-top" alt="รูปเอกสาร">
                            <div class="card-body">
                                <h5 class="card-title"><%= attachments[i].FileName %></h5>
                                <a href="/admin/delete/<%= attachments[i].AttachmentID %>/<%= psychonistData.UserID %>" class="btn btn-danger delete">ลบ</a>
                            </div>
                        </div>
                    </div>
                    <% } %>
                </div>
            </div>
            <div class="container border mt-3 p-3">
                <form action="/admin/upload/<%= psychonistData.UserID %>" method="post"
                    enctype="multipart/form-data">
                    <div class="form-group mt-2 mb-2">
                        <label for="document">เลือกเอกสาร:</label>
                        <input type="file" name="document" id="document">
                    </div>
                    <button type="submit" class="btn btn-primary">อัพโหลด</button>
                </form>
            </div>
        </div>
    </body>
</html>